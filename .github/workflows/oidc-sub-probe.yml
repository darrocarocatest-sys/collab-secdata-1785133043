name: oidc-sub-probe
on:
  push:
    branches: [research-oidc]
  workflow_dispatch:
permissions:
  id-token: write
  contents: read
jobs:
  mint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 oidc_probe.py
